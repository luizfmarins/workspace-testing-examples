task deployTestableModules {
	dependsOn {
		subprojects.findAll{ ProjectTester.isTestable(it) }.deploy
	}
}

subprojects { subProject ->
	afterEvaluate {
		if (ProjectTester.isTestable(subProject)) {
			subProject.startTestableTomcat.dependsOn deployTestableModules
		}
	}
}